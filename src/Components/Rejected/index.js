import React from "react";
import Candidates from "../Candidates";

const Rejected = ({ list }) => {
  return (
    <div>
      <Candidates list={list} />
    </div>
  );
};

export default Rejected;
